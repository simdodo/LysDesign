---
interface Props {
  label: string;
  description?: string;
  className?: string;
}

const { label, description, className = "section-kicker" } = Astro.props;
---

<div class={`page-label-bar ${className}`}>
  <span>{label}</span>
  {description && <p>{description}</p>}
</div>
