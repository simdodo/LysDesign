---
interface Props {
  items: string[];
  ariaLabel?: string;
}

const { items, ariaLabel = "Experience summary" } = Astro.props as Props;
---

<section class="credibility-strip" aria-label={ariaLabel}>
  {
    items.map((item) => (
      <span>{item}</span>
    ))
  }
</section>
