---
interface Props {
  label: string;
  detail?: string;
}

const { label, detail } = Astro.props as Props;
---

<div class="signal-divider" aria-hidden={detail ? "false" : "true"}>
  <span>{label}</span>
  {detail && <p>{detail}</p>}
</div>
